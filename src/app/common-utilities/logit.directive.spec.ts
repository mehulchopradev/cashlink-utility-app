import { LogitDirective } from './logit.directive';

describe('LogitDirective', () => {
  it('should create an instance', () => {
    const directive = new LogitDirective();
    expect(directive).toBeTruthy();
  });
});
